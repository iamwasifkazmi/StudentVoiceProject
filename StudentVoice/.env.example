# Optional: for future env tooling. The app reads the API URL from
# `src/constants/config.ts` (defaults: iOS localhost, Android 10.0.2.2:3000).
# On a physical device, edit that file to your machine's LAN IP or production URL.

API_BASE_URL=http://localhost:3000/api
